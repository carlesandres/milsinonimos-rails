/*global describe, it, after, before, define, sinon, should, be */
'use strict';

define([ 'jquery', 'backbone', 'collections/recentSearches', 'views/recentSearches-view'], function ($, Backbone, recentSearches, recentSearches-view) {

        'use strict';

        describe('recentSearches view', function( ) {
            before( function () {
            } );

            after( function () {
            } );

            it('adds an element when the recentSearches collection adds an element');
            it('deletes an element when the recentSearches collection deletes an element');
        });
    });

